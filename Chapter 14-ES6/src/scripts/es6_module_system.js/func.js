// ! if we want to export every function from this file we should add {export} keyword to function's starting name... if we don't add export keyword this function will be native function for this file and it can't accessible from other files
export const add = (a, b) => a + b

export const sub = (a, b) => a - b

export const into = (a, b) => a * b

export const div = (a, b) => a / b